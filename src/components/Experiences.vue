<template>
  <section id="experiences" class="section">
    <div class="container">
      <h2 class="title">{{ experiences.title }}</h2>

      {% for experience in experiences.experiences %}
      <div class="experience">
        <div class="name">{{ experience.name }}</div>
        <div class="org">
          <a href="{{ experience.org_link }}" target="_blank"
            ><span>{{ experience.org_name }}</span></a
          >
        </div>
        <div class="time">
          <span>{{ experience.date_start }}</span> &ndash; <span>{{ experience.date_end }}</span>
        </div>
      </div>
      {% endfor %}
    </div>
  </section>
</template>